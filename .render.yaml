services:
  - type: web
    name: symfony-app
    env: php
    plan: free
    buildCommand: composer install
    startCommand: php -S 0.0.0.0:10000 -t public
    envVars:
      - key: APP_ENV
        value: prod
      - key: APP_SECRET
        value: your_secret_key
